<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.n8tech.taskcan">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />



    <application



        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".SearchActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".ViewProfileActivity" />
        <activity android:name=".MyBidActivity" />
        <activity android:name=".ViewTaskOnMapsActivity" />
        <activity android:name=".ViewCategoryActivity" />
        <activity android:name=".MyTaskActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".ImageSlideShowActivity" />
        <activity android:name=".EditTaskMapActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".TaskDetailActivity" />
        <activity android:name=".EditTaskActivity" />
        <activity android:name=".SignInActivity">
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>